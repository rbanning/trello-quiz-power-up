{
  "name": "trello-quiz-power-up",
  "version": "1.0.0",
  "description": "Trello Power Up that manages a simple multiple choice quiz based on questions from a cards description",
  "main": "index.js",
  "repository": "https://github.com/rbanning/trello-quiz-power-up.git",
  "author": "rbanning <rob@myhallpass.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc && parcel build src/html/*.html",
    "lint": "tslint -p ./",
    "lintES": "eslint -c .eslintrc.js --ext .ts ./src"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-interpolate-env": "^0.0.1",
    "parcel-plugin-static-files-copy": "^2.5.1",
    "showdown": "^1.9.1"
  },
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^4.1.5"
  }
}
